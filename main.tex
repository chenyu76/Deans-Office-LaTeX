\documentclass[12pt,a4paper]{ctexart}

% 配置
\newlength{\PageMargin}
\setlength{\PageMargin}{1in}      % 全局外框边距（距离纸张边缘的距离）

\newlength{\ContentGap}
\setlength{\ContentGap}{0.1in}    % 外框与页面内容之间的距离距

\newlength{\ContentMargin}
\setlength{\ContentMargin}{\dimexpr\PageMargin + \ContentGap\relax}

\newlength{\RowHeight}
\setlength{\RowHeight}{1.2em}     % 默认行高（最小高度）

\usepackage[
  margin=\ContentMargin,
  top=\ContentMargin,
  bottom=\ContentMargin,
  nomarginpar
]{geometry}
\usepackage{tikz}
\usepackage{xltabular}  % 跨页长表格
\usepackage{tabularx}   % 单行多列计算
\usepackage{atbegshi}   % 每页画框
\usepackage{calc}       % 长度计算
\usepackage{environ}    % 更好的环境定义
\usepackage{changepage} % 局部调整页边距，引入 adjustwidth 环境
\usepackage{lipsum}     % 插入示例内容

% 计算表格内容的总宽度 (版心宽度)
\newlength{\ContentWidth}
\setlength{\ContentWidth}{\dimexpr\textwidth+\ContentGap+\ContentGap\relax}

% 计算是单页排版还是多页排版用到的奇妙工具
\newcommand{\pageLabel}[1]{\label{page:#1}}
\makeatletter
\newcommand{\ifsamePage}[4]{%
  \edef\@A{r@page:#1}%
  \edef\@B{r@page:#2}%
  \ifcsname\@A\endcsname
  \ifcsname\@B\endcsname
  \def\@extract##1##2\@nil{##2}%
  \expandafter\edef\expandafter\@pA\expandafter
  {\expandafter\expandafter\expandafter\@extract\csname\@A\endcsname\@nil}%
  \expandafter\edef\expandafter\@pB\expandafter
  {\expandafter\expandafter\expandafter\@extract\csname\@B\endcsname\@nil}%
  \ifx\@pA\@pB #3\else #4\fi
  \else #4\fi
  \else #4\fi
}
\makeatother
\newcounter{SidebarTableLabelCounter}
\newcounter{TableFrameLabelCounter}

% 侧边栏
% 宽度
\newlength{\SidebarWidth}
\setlength{\SidebarWidth}{0cm}
\newcounter{SidebarEnvCounter}
\def\currentSidebarState{0}% 当前页输出时使用的状态（0=关，1=开）
\def\pendingSidebarState{0}% 用户设定的待生效状态
\newcommand{\EnableSidebar}{%
  \global\def\pendingSidebarState{1}%
  % 注意：不修改 \currentSidebarState
}
\newcommand{\DisableSidebar}{%
  \global\def\pendingSidebarState{0}%
  \global\def\currentSidebarState{0}% 强制当前页输出时不画
}

% 右侧侧边栏
\newlength{\SidebarWidthR}
\setlength{\SidebarWidthR}{0cm}
\newcounter{SidebarEnvCounterR}
\def\currentSidebarStateR{0}
\def\pendingSidebarStateR{0}
\newcommand{\EnableSidebarR}{%
  \global\def\pendingSidebarStateR{1}%
}
\newcommand{\DisableSidebarR}{%
  \global\def\pendingSidebarStateR{0}%
  \global\def\currentSidebarStateR{0}%
}

% 页面外框
\def\currentPageFrameState{0}% 当前页输出时使用的状态（0=关，1=开）
\def\pendingPageFrameState{0}% 用户设定的待生效状态
\newcommand{\EnablePageFrame}{%
  \global\def\pendingPageFrameState{1}%
}
\newcommand{\DisablePageFrame}{%
  \global\def\pendingPageFrameState{0}%
  \global\def\currentPageFrameState{0}%
}

% 绘制全页外框 (Hook到每页输出)
\newcommand{\DrawFullPageFrame}{%
  \begin{tikzpicture}[overlay, remember picture]
    \draw
    ([xshift=\PageMargin, yshift=-\PageMargin]current page.north west)
    rectangle
    ([xshift=-\PageMargin, yshift=\PageMargin]current page.south east);
  \end{tikzpicture}%
}

% 页面外框首半帧（从当前位置向下画到页底，加顶横线）
\newcounter{PageEnvCounter}
\newcommand{\DrawPageFrameHead}{%
  \begin{tikzpicture}[overlay, remember picture]
    \path (current page.west);
    \pgfgetlastxy{\westx}{\dummy};
    \path (current page.east);
    \pgfgetlastxy{\eastx}{\dummyy};
    \path (0,0);
    \pgfgetlastxy{\dummyx}{\currenty}
    \path (current page.south);
    \pgfgetlastxy{\dummyx}{\southy}
    \pgfmathsetlengthmacro{\m}{\PageMargin}
    \draw
    ({\westx + \m}, 0) rectangle ({\eastx - \m}, {\southy - \currenty + \m});
  \end{tikzpicture}%
}

% 页面外框末半帧（从当前位置向上画到页顶，加底横线封口）
\newcommand{\DrawPageFrameTail}{%
  \begin{tikzpicture}[overlay, remember picture]
    \path (current page.west);
    \pgfgetlastxy{\westx}{\dummyy};
    \path (current page.east);
    \pgfgetlastxy{\eastx}{\dummmyy};
    \path (0,0);
    \pgfgetlastxy{\dummyx}{\currenty}
    \path (current page.north);
    \pgfgetlastxy{\dummyx}{\northy}
    \pgfmathsetlengthmacro{\m}{\PageMargin}
    \draw
    ({\westx + \m}, 0) rectangle ({\eastx - \m}, {\northy + \currenty - \m});
  \end{tikzpicture}%
}

\AtBeginShipout{%
  \ifnum\currentPageFrameState=1
  \AtBeginShipoutAddToBox{\DrawFullPageFrame}%
  \fi
  \ifnum\currentSidebarState=1
  \AtBeginShipoutAddToBox{\DrawSidebarVerticalLine}%
  \fi
  \ifnum\currentSidebarStateR=1
  \AtBeginShipoutAddToBox{\DrawSidebarVerticalLineR}%
  \fi
  \xdef\currentSidebarState{\pendingSidebarState}%
  \xdef\currentPageFrameState{\pendingPageFrameState}%
  \xdef\currentSidebarStateR{\pendingSidebarStateR}%
}

% 命令：绘制横向分割线
\newcommand{\SepLine}{%
  \par
  \nointerlineskip % 防止垂直间距干扰
  \noindent
  \begin{tikzpicture}[overlay, remember picture]
    \pgfmathsetlengthmacro{\m}{\PageMargin};
    % \pgfmathsetmacro{\h}{-1};
    % \path (current page.center);
    % \pgfgetlastxy{\centerx}{\centery}; % 提取 x 和 y 坐标
    \path (current page.west);
    \pgfgetlastxy{\westx}{\westy};
    \path (current page.east);
    \pgfgetlastxy{\eastx}{\easty};
    \draw ({\westx + \m + \SidebarWidth}, 0) -- ({\eastx - \m -
    \SidebarWidthR}, 0);
  \end{tikzpicture}
  \par
  \nointerlineskip
}

% 绘制带有垂直间距的分隔线
\newcommand{\vspaceSepLine}{
  \vspace{0.5em}

  \SepLine

  \vspace{0.5em}
}

% 命令：多列行
% 用法: \TableRow[最小高度]{列格式}{内容1 & 内容2...}
% 示例: \TableRow{X|c|X}{ 左边 & 中间 & 右边 }
% 注意：列格式不需要写两边的竖线，系统会自动处理
\NewDocumentCommand{\TableRow}{O{\RowHeight} m m}{%
  \noindent
  \SepLine % 一条分割线
  % 使用 tabularx 占满行宽
  % @{} 消除左右默认间距，使表格紧贴版心边缘
  \begin{tabularx}{\textwidth}{@{} #2 @{}}
    % 在每个单元格内插入一个不可见的rule撑开高度
    \rule{0pt}{#1} #3
  \end{tabularx}%
  \par
  \nointerlineskip % 紧接着画线或下一行
  \SepLine % 每行结束后自动画分割线
}

\NewEnviron{TableFrame}{%
  \stepcounter{TableFrameLabelCounter}
  \par
  \noindent
  \setlength{\parindent}{0pt}
  \setlength{\parskip}{0pt}
  \ifsamePage{TableFrameStart\arabic{TableFrameLabelCounter}}{TableFrameEnd\arabic{TableFrameLabelCounter}}{%
    \stepcounter{PageEnvCounter}%
    \begin{tikzpicture}[overlay, remember picture]
      \path (current page.west);
      \pgfgetlastxy{\westx}{\dummy};
      \path (current page.east);
      \pgfgetlastxy{\eastx}{\dummyy};
      \coordinate (pageStart\thePageEnvCounter) at (0,0);
    \end{tikzpicture}%
  }{%
    \DrawPageFrameHead
    \EnablePageFrame
  }%
  \par
  \nointerlineskip
  \pageLabel{TableFrameStart\arabic{TableFrameLabelCounter}}
  \BODY
  \pageLabel{TableFrameEnd\arabic{TableFrameLabelCounter}}
  \par
  \nointerlineskip
  \noindent
  \ifsamePage{TableFrameStart\arabic{TableFrameLabelCounter}}{TableFrameEnd\arabic{TableFrameLabelCounter}}{%
    \begin{tikzpicture}[overlay, remember picture]
      \path (current page.west);
      \pgfgetlastxy{\westx}{\dummyy};
      \path (current page.east);
      \pgfgetlastxy{\eastx}{\dummmyy};
      \pgfmathsetlengthmacro{\m}{\PageMargin}
      \coordinate (pageEnd\thePageEnvCounter) at (0,0);
      \path (pageStart\thePageEnvCounter);
      \pgfgetlastxy{\dummyx}{\startY}
      \path (pageEnd\thePageEnvCounter);
      \pgfgetlastxy{\endX}{\endY}
      \draw
      ({\westx + \m}, \startY) rectangle ({\eastx - \m}, \endY);
    \end{tikzpicture}%
  }{%
    \DisablePageFrame
    \DrawPageFrameTail
  }%
  \par
}

\NewEnviron{SidebarTable}[3][]{%
  \stepcounter{SidebarTableLabelCounter}
  \xdef\@sideMultiTitle{#2}%
  \def\@tmpa{#1}%
  \ifx\@tmpa\@empty\else
  \xdef\@sideMultiTitle{#1}%
  \fi
  \SepLine{}
  \par
  \nointerlineskip
  \noindent
  \setlength{\SidebarWidth}{#3}

  \ifsamePage{SidebarTableStart\arabic{SidebarTableLabelCounter}}{SidebarTableEnd\arabic{SidebarTableLabelCounter}}{%
    \stepcounter{SidebarEnvCounter}%
    \begin{tikzpicture}[overlay, remember picture]
      \path (current page.west);
      \pgfgetlastxy{\westx}{\dummy};
      \coordinate (sidebarStart\theSidebarEnvCounter) at (0,0);
    \end{tikzpicture}%
  }{%
    \begin{tikzpicture}[overlay, remember picture]
      \path (current page.west);
      \pgfgetlastxy{\westx}{\dummy};
      \path (0,0);
      \pgfgetlastxy{\dummyx}{\currenty}
      \path (current page.south);
      \pgfgetlastxy{\dummyx}{\southy}
      \pgfmathsetlengthmacro{\w}{\SidebarWidth}
      \pgfmathsetlengthmacro{\m}{\PageMargin}
      \pgfmathsetlengthmacro{\l}{\m + \westx + \w}
      \pgfmathsetlengthmacro{\barcx}{\m + \westx + \w / 2}
      \pgfmathsetlengthmacro{\barcy}{(\southy - \currenty + \m)/2}
      \draw (\l, 0) -- (\l, {\southy - \currenty + \m});
      \node at (\barcx, \barcy) {
        \begin{minipage}{#3}
          #2
        \end{minipage}
      };
    \end{tikzpicture}%
    \EnableSidebar%
  }%
  \begin{adjustwidth}{#3}{0cm}
    \pageLabel{SidebarTableStart\arabic{SidebarTableLabelCounter}}
    \BODY
    \pageLabel{SidebarTableEnd\arabic{SidebarTableLabelCounter}}
  \end{adjustwidth}%
  \ifsamePage{SidebarTableStart\arabic{SidebarTableLabelCounter}}{SidebarTableEnd\arabic{SidebarTableLabelCounter}}{%
    \begin{tikzpicture}[overlay, remember picture]
      \path (current page.west);
      \pgfgetlastxy{\westx}{\dummyy};
      \path (current page.east);
      \pgfgetlastxy{\eastx}{\dummmyy};
      \coordinate (sidebarEnd\theSidebarEnvCounter) at (0,0);
      \path (sidebarStart\theSidebarEnvCounter);
      \pgfgetlastxy{\dummyx}{\startY}
      \path (sidebarEnd\theSidebarEnvCounter);
      \pgfgetlastxy{\endX}{\endY}
      \pgfmathsetlengthmacro{\w}{\SidebarWidth}
      \pgfmathsetlengthmacro{\m}{\PageMargin}
      \pgfmathsetlengthmacro{\l}{\m + \westx + \w}
      \pgfmathsetlengthmacro{\barcx}{\m + \westx + \w / 2}
      \pgfmathsetlengthmacro{\midY}{(\startY + \endY) / 2}
      \draw (\l, \startY) -- (\l, \endY);
      \draw ({\westx + \m}, \endY) -- ({\eastx - \m}, \endY);
      \node at (\barcx, \midY) {
        \begin{minipage}{#3}
          #2
        \end{minipage}
      };
    \end{tikzpicture}%
  }{%
    \DisableSidebar%
    \begin{tikzpicture}[overlay, remember picture]
      \path (current page.west);
      \pgfgetlastxy{\westx}{\dummyy};
      \path (0,0);
      \pgfgetlastxy{\dummyx}{\currenty}
      \path (current page.north);
      \pgfgetlastxy{\dummyx}{\northy}
      \path (current page.east);
      \pgfgetlastxy{\eastx}{\dummmyy};
      \pgfmathsetlengthmacro{\w}{\SidebarWidth}
      \pgfmathsetlengthmacro{\m}{\PageMargin}
      \pgfmathsetlengthmacro{\l}{\m + \westx + \w}
      \draw (\l, 0) -- (\l, {\northy + \currenty - \m});
      \draw ({\westx + \m}, 0) -- ({\eastx - \m}, 0);
      \pgfmathsetlengthmacro{\barcx}{\m + \westx + \w / 2}
      \pgfmathsetlengthmacro{\barcy}{(\northy + \currenty - \m)/2}
      \node at (\barcx, \barcy) {
        \begin{minipage}{#3}
          #1
        \end{minipage}
      };
    \end{tikzpicture}%
  }%
  \par
  \nointerlineskip
  \noindent
  \setlength{\SidebarWidth}{0cm}
}

\newcounter{SidebarTableLabelCounterR}
\NewEnviron{SidebarTableR}[3][]{%
  \stepcounter{SidebarTableLabelCounterR}
  \xdef\@sideMultiTitleR{#2}%
  \def\@tmpa{#1}%
  \ifx\@tmpa\@empty\else
  \xdef\@sideMultiTitleR{#1}%
  \fi
  \SepLine{}
  \par
  \nointerlineskip
  \noindent
  \setlength{\SidebarWidthR}{#3}

  \ifsamePage{SidebarTableRStart\arabic{SidebarTableLabelCounterR}}{SidebarTableREnd\arabic{SidebarTableLabelCounterR}}{%
    \stepcounter{SidebarEnvCounterR}%
    \begin{tikzpicture}[overlay, remember picture]
      \path (current page.east);
      \pgfgetlastxy{\eastx}{\dummy};
      \coordinate (sidebarRStart\theSidebarEnvCounterR) at (0,0);
    \end{tikzpicture}%
  }{%
    \begin{tikzpicture}[overlay, remember picture]
      \path (current page.east);
      \pgfgetlastxy{\eastx}{\dummy};
      \path (0,0);
      \pgfgetlastxy{\dummyx}{\currenty}
      \path (current page.south);
      \pgfgetlastxy{\dummyx}{\southy}
      \pgfmathsetlengthmacro{\w}{\SidebarWidthR}
      \pgfmathsetlengthmacro{\m}{\PageMargin}
      \pgfmathsetlengthmacro{\l}{\eastx - \m - \w}
      \pgfmathsetlengthmacro{\barcx}{\eastx - \m - \w / 2}
      \pgfmathsetlengthmacro{\barcy}{(\southy - \currenty + \m)/2}
      \draw (\l, 0) -- (\l, {\southy - \currenty + \m});
      \node at (\barcx, \barcy) {
        \begin{minipage}{#3}
          #2
        \end{minipage}
      };
    \end{tikzpicture}%
    \EnableSidebarR%
  }%
  \begin{adjustwidth}{0cm}{#3}
    \pageLabel{SidebarTableRStart\arabic{SidebarTableLabelCounterR}}
    \BODY
    \pageLabel{SidebarTableREnd\arabic{SidebarTableLabelCounterR}}
  \end{adjustwidth}%
  \ifsamePage{SidebarTableRStart\arabic{SidebarTableLabelCounterR}}{SidebarTableREnd\arabic{SidebarTableLabelCounterR}}{%
    \begin{tikzpicture}[overlay, remember picture]
      \path (current page.west);
      \pgfgetlastxy{\westx}{\dummyy};
      \path (current page.east);
      \pgfgetlastxy{\eastx}{\dummmyy};
      \coordinate (sidebarREnd\theSidebarEnvCounterR) at (0,0);
      \path (sidebarRStart\theSidebarEnvCounterR);
      \pgfgetlastxy{\dummyx}{\startY}
      \path (sidebarREnd\theSidebarEnvCounterR);
      \pgfgetlastxy{\endX}{\endY}
      \pgfmathsetlengthmacro{\w}{\SidebarWidthR}
      \pgfmathsetlengthmacro{\m}{\PageMargin}
      \pgfmathsetlengthmacro{\l}{\eastx - \m - \w}
      \pgfmathsetlengthmacro{\barcx}{\eastx - \m - \w / 2}
      \pgfmathsetlengthmacro{\midY}{(\startY + \endY) / 2}
      \draw (\l, \startY) -- (\l, \endY);
      \draw ({\westx + \m}, \endY) -- ({\eastx - \m}, \endY);
      \node at (\barcx, \midY) {
        \begin{minipage}{#3}
          #2
        \end{minipage}
      };
    \end{tikzpicture}%
  }{%
    \DisableSidebarR%
    \begin{tikzpicture}[overlay, remember picture]
      \path (current page.west);
      \pgfgetlastxy{\westx}{\dummyy};
      \path (current page.east);
      \pgfgetlastxy{\eastx}{\dummmyy};
      \path (0,0);
      \pgfgetlastxy{\dummyx}{\currenty}
      \path (current page.north);
      \pgfgetlastxy{\dummyx}{\northy}
      \pgfmathsetlengthmacro{\w}{\SidebarWidthR}
      \pgfmathsetlengthmacro{\m}{\PageMargin}
      \pgfmathsetlengthmacro{\l}{\eastx - \m - \w}
      \draw (\l, 0) -- (\l, {\northy + \currenty - \m});
      \draw ({\westx + \m}, 0) -- ({\eastx - \m}, 0);
      \pgfmathsetlengthmacro{\barcx}{\eastx - \m - \w / 2}
      \pgfmathsetlengthmacro{\barcy}{(\northy + \currenty - \m)/2}
      \node at (\barcx, \barcy) {
        \begin{minipage}{#3}
          #1
        \end{minipage}
      };
    \end{tikzpicture}%
  }%
  \par
  \nointerlineskip
  \noindent
  \setlength{\SidebarWidthR}{0cm}
}

\newcommand{\DrawSidebarVerticalLine}{
  \begin{tikzpicture}[overlay, remember picture]
    \path (current page.west);
    \pgfgetlastxy{\westx}{\dummy};
    \path (0,0);
    \pgfgetlastxy{\dummyx}{\currenty}
    \path (current page.south);
    \pgfgetlastxy{\dummyx}{\southy}
    \path (current page.north);
    \pgfgetlastxy{\dummyx}{\northy}
    \pgfmathsetlengthmacro{\w}{\SidebarWidth}
    \pgfmathsetlengthmacro{\m}{\PageMargin}
    \pgfmathsetlengthmacro{\l}{\m + \westx + \w}
    \pgfmathsetlengthmacro{\barcx}{\m + \westx + \w / 2}
    \pgfmathsetlengthmacro{\barcy}{(\southy + \northy) / 2}
    \draw (\l, {\southy - \currenty + \m}) -- (\l, {\northy + \currenty - \m});
    \node at (\barcx, \barcy) {
      \begin{minipage}{\SidebarWidth}
        \centering
        \@sideMultiTitle
      \end{minipage}
    };
  \end{tikzpicture}%
}

\newcommand{\DrawSidebarVerticalLineR}{
  \begin{tikzpicture}[overlay, remember picture]
    \path (current page.east);
    \pgfgetlastxy{\eastx}{\dummy};
    \path (0,0);
    \pgfgetlastxy{\dummyx}{\currenty}
    \path (current page.south);
    \pgfgetlastxy{\dummyx}{\southy}
    \path (current page.north);
    \pgfgetlastxy{\dummyx}{\northy}
    \pgfmathsetlengthmacro{\w}{\SidebarWidthR}
    \pgfmathsetlengthmacro{\m}{\PageMargin}
    \pgfmathsetlengthmacro{\l}{\eastx - \m - \w}
    \pgfmathsetlengthmacro{\barcx}{\eastx - \m - \w / 2}
    \pgfmathsetlengthmacro{\barcy}{(\southy + \northy) / 2}
    \draw (\l, {\southy - \currenty + \m}) -- (\l, {\northy + \currenty - \m});
    \node at (\barcx, \barcy) {
      \begin{minipage}{\SidebarWidthR}
        \centering
        \@sideMultiTitleR
      \end{minipage}
    };
  \end{tikzpicture}%
}

%========================%
%                        %
%      文档正文开始      %
%                        %
%========================%
\begin{document}
% 表头
\vspace{0.5em}
{\centering\bfseries\LARGE 本科毕业设计（论文）开题报告 \par}
\vspace{0.3em}
{\centering\large XX大学 · 2077届 \par}
% 开始带框页面
\begin{TableFrame}
  % 单行多列信息
  % 语法：\TableRow{列格式}{内容}
  % X 代表自动宽度，c 代表居中，l/r 分别是左/右对齐，| 代表竖线
  % 在内容中，使用 & 分隔单元
  \TableRow{c|X}{
    \textbf{题\quad\quad 目} & 基于LaTeX的通用表格模板设计与实现
  }

  \TableRow{ c | X | c | X }{
    \textbf{姓\quad\quad 名} & 张三 & \textbf{学\quad 号} & 2073123456
  }

  \TableRow{ c | X | c | X }{
    \textbf{学\quad\quad 院} & 计算机与软件学院 & \textbf{专\quad 业}
    & 计算机科学
  }

  \TableRow{ c | X | c | X }{
    \textbf{指导教师} & 王教授 & \textbf{职\quad 称} & 教授
  }

  % 标题栏，l 表示左对齐
  \TableRow{l}{ \textbf{一、本选题的意义及国内外发展状况：} }

  % 对于正文非常长的部分，不用 \TableRow，直接写内容，
  \vspace{0.5em} % 这个命令创建了 0.5em 高的垂直间距。
  % em是长度单位，1em长度基本与文本中一个汉字的高度相同。
  % 在本模板中，由于我技术有限，我们需要经常使用这个命令控制间距，请留意。

  样式修改后可能需要第三次编译边框线才能正确显示。

  \vspace{4cm} % 一个4cm 的垂直间距

  \TableRow{l}{ \textbf{二、研究内容：} }

  \begin{SidebarTable}{
      \centering
      支持侧边标题
    }{1cm} % 1cm是侧边栏的宽度

    \vspace{0.5em}

    这里是研究内容的正文。由于我们使用的是 overlay 画外框，这里的文本可
    以像普通 LaTeX 文档一样编写。

    可以随意使用列表：
    \begin{itemize}
      \item 这是一个列表项
      \item 这是另一个列表项
    \end{itemize}

    也可以显示公式：
    \begin{equation}
      e^{\pi i} + 1 = 0
    \end{equation}

    下面这个 \texttt{lipsum} 命令会插入一大堆内容，用于在PDF中展示换页的效果。
    \lipsum[1-2] % 插入一些假文

    \vspace{0.5em}

  \end{SidebarTable}

  % 结尾签字栏
  \TableRow{X | X}{
    \centering \textbf{指导教师意见} & \centering \textbf{学院审核意见}
  }
  % 空白栏，使用vspace 设置高度
  \TableRow[4cm]{X | X}{
    \vspace{2.5cm} & \vspace{2.5cm}
  }

  \vspace{0.5em}

  可以使用\texttt{\textbackslash{}vspaceSepLine}命令来画一条带间距的分割线：

  \vspaceSepLine

  像上面那条。

  \texttt{TableFrame} 环境结束前如果是文本需要添加一个
  \texttt{vspace}，否则底下的边框线会和文本重叠。

  \vspace{0.5em}

\end{TableFrame}

\texttt{TableFrame}环境外面的内容不带框。

后面是更多的一些示例。

\begin{TableFrame}

  % 技巧：通过调整 X 的系数 (如 >{\hsize=0.5\hsize}X ) 可以控制列宽比例
  \TableRow{X|>{\hsize=0.5\hsize}X}{
    B & D
  }
  \TableRow{>{\hsize=0.5\hsize}X|X}{
    A & C
  }
  \TableRow{l|X|X}{
    行1 & 内容A & 内容B
  }
  \TableRow{l|X}{
    行2 & 内容C
  }

  \begin{SidebarTable}[
      \centering
      侧

      边

      栏

      续
    ]{
      \centering
      侧

      边

      栏
    }{1cm}
    侧边栏跨页时可以使用可选参数指定第二页及其之后的页码的侧边栏的标题。

    \lipsum[1-1]

    \vspace{0.5em}

    \TableRow{X|X}{
      此外，& 也可以
    }
    \TableRow{X|X}{
      \centering 在侧边栏中 \newline  % 在表格中使用 \newline 换行
      添加表格。 &
    }
    \vspace{0.5em}

    和

    \vspaceSepLine{}

    使用分隔线。
  \end{SidebarTable}

  \begin{SidebarTableR}[h]{
      \centering
      照片

      位置
    }{4cm}
    最后，还有侧边栏在右侧的版本\texttt{SidebarTableR} 。这也许可以用来放照片
  \end{SidebarTableR}
\end{TableFrame}
\end{document}
